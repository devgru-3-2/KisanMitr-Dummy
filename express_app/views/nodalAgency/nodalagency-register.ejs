<!DOCTYPE html>
<html>
  <head>
    <title>Nodal Agency Registration</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f5f5f5;
      }
      h1 {
        color: #333;
        text-align: center;
        margin-top: 40px;
      }
      form {
        background-color: #fff;
        max-width: 500px;
        margin: 0 auto;
        padding: 20px;
        border-radius: 5px;
        box-shadow: 0 5px 20px rgba(0,0,0,0.1);
      }
      label {
        display: inline-block;
        font-weight: bold;
        margin-bottom: 10px;
      }
      input[type="text"],
      input[type="tel"] {
        display: block;
        width: 100%;
        padding: 10px;
        border: none;
        border-radius: 3px;
        margin-bottom: 20px;
      }
      input[type="submit"] {
        background-color: #4CAF50;
        color: #fff;
        padding: 10px 20px;
        border: none;
        border-radius: 3px;
        cursor: pointer;
      }
      input[type="submit"]:hover {
        background-color: #3e8e41;
      }
    </style>
  </head>
  <body>
    <h1>Nodal Agency Registration</h1>
    <form method="POST" action="/nodal-agency/register">
      <label for="name">Name:</label>
      <input type="text" name="name" required>
      <label for="phone">Phone:</label>
      <input type="tel" name="phone" pattern="[0-9]{10}" required>
      <label for="address">Zipcode:</label>
      <input type="text" name="address.zipcode" pattern="[0-9]{6}" required>
      <input type="submit" value="Submit">
    </form>
  </body>
</html>
